# Client-side environment variables for RedeemDust

# Polymarket API endpoint (defaults to "https://data-api.polymarket.com/positions" if not set)
VITE_POSITIONS_ENDPOINT=https://data-api.polymarket.com/positions

# Minimum size threshold for dust positions (defaults to 0 if not set)
VITE_SIZE_THRESHOLD_DEFAULT=0

# Custom RPC URL for Polygon network (optional)
VITE_RPC_URL=https://polygon-rpc.com